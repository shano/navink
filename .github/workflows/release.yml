name: Release

on:
  push:
    branches: [master]
    tags: ["v*"]
  pull_request:
    branches: [master]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout navink
        uses: actions/checkout@v4
        with:
          path: navink

      - name: Checkout MMD
        uses: actions/checkout@v4
        with:
          repository: mudita/MMD
          path: MMD

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Build debug APK
        working-directory: navink
        run: ./gradlew assembleDebug --no-daemon

      - name: Rename APK to release version
        if: startsWith(github.ref, 'refs/tags/v')
        working-directory: navink/app/build/outputs/apk/debug
        run: mv app-debug.apk "navink-${GITHUB_REF_NAME#v}.apk"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: navink-apk
          path: navink/app/build/outputs/apk/debug/*.apk

      - name: Create release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: navink/app/build/outputs/apk/debug/navink-*.apk
          generate_release_notes: true
